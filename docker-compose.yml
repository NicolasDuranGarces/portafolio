version: '3.8'
services:
  web:
    image: portfolio:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio
    ports:
      - "2320:80"
    restart: unless-stopped
